---
import Layout from '@layouts/Layout.astro';
import BookList from '@pages/fragments/BookList/index.astro';
import { metadata } from '@config/metadata.js';

const pageTitle = `Books — ${metadata.name}`;
const pageDescription = `A collection of favorite books. ${metadata.defaultDescription}`;
---

<Layout title={pageTitle} description={pageDescription}>
  <BookList />
</Layout>
